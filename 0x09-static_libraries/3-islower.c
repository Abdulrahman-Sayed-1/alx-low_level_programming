#include "main.h"
/**
 * _islower - checks if a character is uppercase or not.
 * 
 * @c: the character to be checkecked
 * 
 * Return: 1 if c is lowercase, 0 otherwise.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
