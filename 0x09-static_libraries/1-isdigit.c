#include "main.h"
/**
 * _isdigit - checks if a character is a digit or not (0 through 9).
 *
 * @c: int to be checked.
 *
 * Return: 1 if c is a digit, otherwise 0.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}


