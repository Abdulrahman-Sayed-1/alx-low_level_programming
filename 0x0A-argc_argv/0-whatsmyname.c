#include "main.h"
/**
 * main - Prints its name.
 *
 * @argc: The number of command line arguments.
 * @argv: The argument of the commmand line.
 *
 * Return: on success 0, on error -1.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

